interface Props {
  text: string;
}

function WorkDisplay({ text }: Props) {
  const evenStyle = {};

  const oddStyle = {};

  return (
    <div className="WorkDisplay">
      <img></img>
    </div>
  );
}

export default WorkDisplay;
